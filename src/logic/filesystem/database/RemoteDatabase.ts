import Database, { Statement } from "better-sqlite3";

export default class RemoteDatabase {
    constructor(dbLocation: string, options: Database.Options) {

    }

    run(query: string, args: any[]): void {

    }

    getRows(query: string, args: any[]): any[] {

    }

    getRow(query: string, args: any[]): any {

    }
}
